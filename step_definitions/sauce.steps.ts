import { Given, When, Then } from '@cucumber/cucumber';

Given('que estoy en la página de login de SauceDemo', async function () {});
When('ingreso con usuario {string} y contraseña por defecto', async function () {});
Then('debería ver el catálogo de productos', async function () {});
